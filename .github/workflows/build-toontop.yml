name: Build ToonTop APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/en/toontop/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: List available tasks for toontop
        run: ./gradlew :src:en:toontop:tasks --all 2>&1 | grep -i "assemble\|build\|apk" || true

      - name: Build ToonTop APK
        run: ./gradlew :src:en:toontop:assembleRelease --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ToonTop-APK
          path: src/en/toontop/build/outputs/apk/**/*.apk
          if-no-files-found: warn
